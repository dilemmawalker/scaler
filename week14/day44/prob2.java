public class Solution {
    public int solve(int[] arr) {
        int n=arr.length;
        int[]arrb=new int[n];
        Stack<Integer>st=new Stack<>();
        for(int i=0;i<n;i++){
            int val=arr[i];
            if(st.size()==)
        }
    }
}
